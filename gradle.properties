org.gradle.jvmargs=-Xmx2G
# Where your own copies live. Never committed, never downloaded by this build.
# Override in local.properties or on the command line:
#   ./gradlew build -PserverJar=/path/to/minecraft-infinite-server.jar
